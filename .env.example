# Copy to .env and edit. docker-compose reads .env automatically.

# Login password (clear text here — backend hashes it with bcrypt at boot)
BACKEND_PASSWORD=changeme

# JWT signing secret — must be IDENTICAL on backend and frontend (Next middleware
# verifies tokens issued by the backend). Use a long random string in prod.
JWT_SECRET=dev-secret-change-me
